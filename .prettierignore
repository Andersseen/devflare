# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.angular
.wrangler
node_modules
pnpm-lock.yaml
.env.sample
.env.example
.dev.vars

# Generated by flowmark from the sibling .flow templates, and gitignored — so
# CI never sees them, but a local `build:flow` would otherwise fail format:check.
*.flow.js
