# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.angular
.wrangler
node_modules
pnpm-lock.yaml
.env.sample
.env.example
.dev.vars

# Generated by flowmark from the sibling .flow templates. They are committed so
# CI can build without the Rust CLI, but they are machine output — never format
# them, and never edit them by hand (edit the .flow source and recompile).
*.flow.js
