{
  "name": "@devflare/dev-auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "dependencies": {
    "@flowview/runtime": "^0.1.2"
  },
  "devDependencies": {
    "@flowview/compiler": "^0.1.2"
  },
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build:flow": "node scripts/compile-flow.mjs",
    "watch:flow": "node scripts/watch-flow.mjs"
  }
}
