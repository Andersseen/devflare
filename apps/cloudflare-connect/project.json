{
  "name": "cloudflare-connect",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/cloudflare-connect/src",
  "tags": ["scope:backend", "type:service", "domain:cloudflare-connect"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/apps/cloudflare-connect"],
      "options": {
        "command": "wrangler deploy --dry-run --outdir=dist/apps/cloudflare-connect",
        "cwd": "apps/cloudflare-connect"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "continuous": true,
      "options": {
        "command": "wrangler dev --local",
        "cwd": "apps/cloudflare-connect"
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "wrangler deploy",
        "cwd": "apps/cloudflare-connect"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.app.json",
        "cwd": "apps/cloudflare-connect"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "options": {
        "config": "apps/cloudflare-connect/vitest.config.ts"
      }
    }
  }
}
