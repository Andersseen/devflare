name: Deploy to Staging

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

# Cloudflare credentials are attached per step rather than to the whole
# workflow. This job runs `pnpm check` alongside the deploy, and the test suite
# must not inherit an account-wide token — with it exported workflow-wide, the
# tests asserting "no credential configured" read the runner's real ones through
# the documented process.env fallback and failed here.

# NOTE: staging covers the auth service only. The Analog app has no staging
# environment yet — that would need a `devflare-db-staging` D1 plus an
# [env.staging] block in apps/devflare/wrangler.toml.
jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/cloudflare-preflight
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - uses: pnpm/action-setup@v6
        with:
          version: 10

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: 'pnpm'

      - run: pnpm install --frozen-lockfile

      - name: Run checks
        run: pnpm check

      # Target the DB *binding*: under `--env staging` the bound database is
      # `dev-auth-db-staging`, so the old `dev-auth-db` argument never resolved.
      - name: Apply D1 migrations (staging)
        working-directory: apps/dev-auth
        run: npx wrangler d1 migrations apply DB --env staging --remote
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Deploy auth service (staging)
        working-directory: apps/dev-auth
        run: npx wrangler deploy --env staging
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Comment PR with staging URL
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v9
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `🚀 Staging deployment ready!\n\nAuth service: https://auth-devflare-staging.andersseen.dev`
            })
